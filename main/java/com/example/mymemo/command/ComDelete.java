/*package com.example.mymemo.command;

import com.example.mymemo.MemoModel;
import com.example.mymemo.MemoView;

public class ComDelete implements MemoCommand{
    private MemoView mView;
    private MemoModel mModel;

    public ComDelete(MemoView view, MemoModel model){//コンストラクタ
        mView = view;
        mModel = model;
    }

    //public void execute(){
       // mModel.delete(mView.getDate(), mView.getTitle(), mView.getText());
       // mModel.searchMemo(mView.getDate(),mView.getTitle(), mView.getText());
   // }
}

 */
